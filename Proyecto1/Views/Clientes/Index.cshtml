@model IEnumerable<Proyecto1.Models.Cliente>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Cliente</h1>

<div class="container mt-4">
    <p class="text-right">
        <a asp-action="Create" class="btn" style="background-color: #f8d3e2; color: #d63384;">Crear Nuevo</a>
    </p>
    <table class="table table-bordered table-striped">
        <thead style="background-color: #f8d3e2;">
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Nombre)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Apellido)
                </th>
                <th class="text-center">Acciones</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Nombre)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Apellido)
                    </td>
                    <td class="text-center">
                        <a asp-action="Edit" asp-route-id="@item.Documento" class="btn" style="background-color: #f8d3e2; color: #d63384;">Editar</a>
                        <a asp-action="Details" asp-route-id="@item.Documento" class="btn btn-info btn-sm">Detalles</a>
                        <a asp-action="Delete" asp-route-id="@item.Documento" class="btn btn-danger btn-sm">Eliminar</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>


